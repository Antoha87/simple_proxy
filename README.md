* Create virtual env <code> python3 -m venv venv </code>
* Activate env  <code> source/bin/activate </code>
* Install requirements <code>  pip install -r requierement.txt </code>
* Start proxy <code>  python proxy.py </code>
